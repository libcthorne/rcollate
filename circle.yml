machine:
  python:
    version: 3.6.0
  environment:
    CONFIG_DIR: "config/tests_config"

test:
  override:
    - nosetests --with-coverage --cover-package=rcollate
  post:
    - bash <(curl -s https://codecov.io/bash)

general:
  branches:
    only:
     - ci
     - master
